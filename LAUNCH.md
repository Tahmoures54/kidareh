# ✅ آماده‌سازی انتشار کی‌داره

این چک‌لیست برای لانچ و جذب کاربر است. با لحن دوستانه برای تیم و کاربر نوشته شده.

---

## قبل از روشن کردن سرور production

- [ ] `JWT_SECRET` و `COOKIE_SECRET` قوی و یکتا
- [ ] `NODE_ENV=production`
- [ ] `PAYPING_TOKEN` واقعی (اگر پرداخت فعال است)
- [ ] کلید پیامک (کاوه نگار یا سرویس شما)
- [ ] `REDIS_URL` در صورت استفاده از کش (اختیاری اما توصیه‌شده)
- [ ] دامنه و `APP_URL` درست در CORS / ALLOWED_ORIGINS
- [ ] فایل‌های `.env` واقعی در git نباشند

## سلامت فنی

```bash
curl -s https://YOUR-DOMAIN/api/health
```

انتظار: `"status":"healthy"`

مسیرهای مهم دستی:

1. ورود با شماره موبایل + OTP  
2. جستجوی کالا در شهر خودتان  
3. ثبت فروشگاه / ثبت یک کالا  
4. چت ساده بین دو حساب  
5. خرید یک پکیج دیده شدن (یا dev-confirm در تست)  
6. بنر اسپانسر روی صفحه اصلی (اگر فعال باشد)

## پیام‌هایی که به کاربر می‌گوییم (لحن)

- کوتاه، محترمانه، بدون اصطلاح فنی  
- به‌جای «خطای سرور» → «الان نت قطع شد؛ یک‌بار دیگر امتحان کن»  
- به‌جای «Unauthorized» → «لطفاً دوباره وارد شو»  
- تشویق ملایم برای فروشنده: «اول یک کالا بگذار؛ بعد دیده می‌شوی»

## جذب کاربر (هفتهٔ اول)

1. **خریدار:** لینک اشتراک‌گذاری داخل اپ + استوری محلی «چی دور و برت هست؟»  
2. **فروشنده:** دعوت حضوری به ۳–۵ مغازهٔ محله + پکیج آزمایش ۳ روزه  
3. **شفافیت:** روی بنر تبلیغاتی حتماً «آگهی» بماند  
4. **پشتیبانی:** پاسخ سریع در `/support` اعتماد می‌سازد

## بعد از لانچ

- لاگ خطاها را روزانه یک‌بار نگاه کنید  
- فروشنده‌هایی که کالا ثبت کردند را با پیام کوتاه تشویق کنید  
- آمار بنر و پکیج‌ها از `/api/promotions/my-stats`

---

با آرزوی فروش خوب برای مغازه‌ها و پیدا کردن راحت کالا برای مشتری‌ها 🌿  
**کی‌داره — ببین کی داره، حضوری بگیر**
